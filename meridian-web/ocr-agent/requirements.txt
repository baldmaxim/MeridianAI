# httpx — асинхронный клиент к серверу Meridian и LM Studio (модель отвечает десятками секунд).
httpx>=0.27
# Рендер страниц PDF в картинки: BSD/Apache, без системных зависимостей.
# PyMuPDF не берём — AGPL не годится для проприетарного продукта.
pypdfium2>=5.13.0
Pillow>=12.3.0
