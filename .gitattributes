# Скрипты уезжают на Linux-хосты (YC, ноды) — CRLF ломает shebang
*.py text eol=lf
*.sh text eol=lf
# Скрипты агента для Windows: cmd/PowerShell ждут CRLF (иначе парсер cmd спотыкается)
*.cmd text eol=crlf
*.ps1 text eol=crlf
